#include <stdio.h>
#include <stdlib.h>
int main()
{

    controle=1;

       for (cont=1;cont<=20;cont++)

       {  scanf ("%d",&num);

          if (controle==1)

            menor=num;

          else

             if (num

                menor=num;

          controle++;

        }

return 0;
}
